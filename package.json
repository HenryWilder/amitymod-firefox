{
  "name": "amitymod-firefox",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build-typescript": "tsc -p src/scripts/tsconfig.json",
    "build-sass": "sass --update --no-source-map src/styles/:dist/styles/ src/styles/content-styles/:dist/content-styles/",
    "build": "astro check && astro build && npm run build-typescript && npm run build-sass",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.3.1",
    "@types/firefox-webext-browser": "^120.0.0",
    "astro": "^4.0.3",
    "sass": "^1.69.5",
    "typescript": "^5.3.3"
  }
}
